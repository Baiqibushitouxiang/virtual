<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sustbbgz.virtualspringbootbackend.mapper.CompositeModelMapper">

    <resultMap id="CompositeModelResultMap" type="com.sustbbgz.virtualspringbootbackend.entity.CompositeModel">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectById" resultMap="CompositeModelResultMap">
        SELECT * FROM composite_models WHERE id = #{id}
    </select>
    
    <select id="selectByName" parameterType="string" resultMap="CompositeModelResultMap">
        SELECT * FROM composite_models WHERE name = #{name}
    </select>
    
    <select id="selectAll" resultMap="CompositeModelResultMap">
        SELECT * FROM composite_models
    </select>

</mapper>